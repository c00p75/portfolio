# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts

# content pipeline
/.velite
/public/static

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# The retrieval index is committed on purpose: it is a build artifact that must
# version and roll back with the content it describes, and committing it means a
# deploy never needs an embedding API key. Regenerate with `npm run ingest`.
